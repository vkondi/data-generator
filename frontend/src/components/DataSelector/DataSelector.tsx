/**
 * DataSelector component that displays the current year and a copyright notice.
 *
 * @returns {JSX.Element} The rendered DataSelector component.
 */

import Layout from "../Layout/Layout";
import "./DataSelector.css";

const DataSelector: React.FC = () => {
  return (
    <Layout>
      <div className="container">Hello World</div>
    </Layout>
  );
};

export default DataSelector;
